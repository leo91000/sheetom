# @sheetom/native-linux-x64-gnu

Platform-specific native engine for [SheetOM](https://www.npmjs.com/package/sheetom) on `linux-x64-gnu`. Install `sheetom`; package managers select this implementation artifact automatically.
