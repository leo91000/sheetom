# @sheetom/native-linux-s390x-gnu

Platform-specific native engine for [SheetOM](https://www.npmjs.com/package/sheetom) on `linux-s390x-gnu`. Install `sheetom`; package managers select this implementation artifact automatically.
