# @sheetom/native-darwin-x64

Platform-specific native engine for [SheetOM](https://www.npmjs.com/package/sheetom) on `darwin-x64`. Install `sheetom`; package managers select this implementation artifact automatically.
