# @sheetom/native-linux-ppc64-gnu

Platform-specific native engine for [SheetOM](https://www.npmjs.com/package/sheetom) on `linux-ppc64-gnu`. Install `sheetom`; package managers select this implementation artifact automatically.
