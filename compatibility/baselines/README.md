# Compatibility baselines

Release pull requests add one immutable, schema-validated `<version>.json` report here. RC6 and later reports identify the exact vendored native-engine source manifest and hash the executed grammar and process-safety evidence. `0.0.0` is an unpublished implementation version and intentionally has no report.
