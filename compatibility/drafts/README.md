# Baseline drafts

Run `SHEETOM_RECORD_BASELINE=1 npm run conformance:record` after all pinned oracle jobs have completed. Review the generated draft, then move it to `compatibility/baselines/<version>.json` in the release pull request.
