# RC6 Typed Value Engine Migration

RC6 replaces the string-driven value path through reviewable pull requests while keeping every intermediate foundation inaccessible from the published interface.

1. Import the exact rust-cssparser source repository and select its crates through local Cargo patches without behavior changes.
2. Implement the SheetOM-owned Recovered Component Value Tree and lock it to CSS Syntax and recovery evidence.
3. Add owned Semantic Property Values, immutable Semantic Declarations and the generated Property Grammar Registry.
4. Route standard property and shorthand parsing plus expansion through owned Vendored Lightning Source ASTs, adding focused upstreamable grammar corrections where required.
5. Implement typed Observable Codecs and structured Reparsable Stylesheet Serialization with disposable caches.
6. Migrate Shadow Engine Runs to the typed path, then remove every raw-string grammar helper and enforce that architecture in CI.
7. Close the complete pinned Chromium grammar inventory, observable mutation sequences, WPT mappings, native reparsing, fuzzing, subprocess crash safety and performance gates.
8. Switch the published interface atomically, delete the replaced engine, prepare exact release evidence and begin the required RC6 soak.

The earlier property-corpus branch is evidence rather than a delivery branch. Vendored corrections are cherry-picked only after individual audit; heuristic SheetOM corrections are rewritten against the typed interfaces.
