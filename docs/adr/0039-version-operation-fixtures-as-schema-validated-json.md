# Version operation fixtures as schema-validated JSON

Operation Fixtures are checked-in JSON documents validated by a checked-in, versioned JSON Schema, using stable object handles and structured adapter observations. This keeps the conformance corpus portable across SheetOM and browser adapters without making executable TypeScript fixtures part of the test interface.
