# Name the package SheetOM

The repository and npm package will be named `sheetom`, a compact expression of “stylesheet object model” that describes the Authoring CSSOM domain without coupling the public identity to Lightning CSS or another parser implementation. The independent package will use the MIT license and acknowledge its parser dependencies without implying official affiliation.
