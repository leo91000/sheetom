# Stabilize diagnostic codes and structured fields

Opt-in diagnostics will expose stable machine-readable codes and structured operation, severity, property, input, and location fields. Human-readable messages may improve without an API-breaking release, and diagnostic collection remains disabled by default.
