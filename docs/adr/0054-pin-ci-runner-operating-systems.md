# Pin CI runner operating systems

CI will use explicit `ubuntu-24.04`, `windows-2025`, and `macos-15` GitHub-hosted runner labels, and runner upgrades will arrive through reviewed changes. Moving `-latest` aliases are excluded from the Compatibility Baseline because they can change operating systems without a repository decision.
