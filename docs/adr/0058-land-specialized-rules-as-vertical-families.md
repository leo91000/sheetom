# Land specialized rules as vertical families

The Rule Tree will deepen incrementally through core hierarchy, grouping, and style nesting; conditions, imports, and `MediaList`; declaration-bearing rules; keyframes; then descriptor maps. Each family replaces its Generic Rule path immediately and passes focused public-interface conformance before the next family begins, avoiding both a monolithic rewrite and parallel state authorities.
