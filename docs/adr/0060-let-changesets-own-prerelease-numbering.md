# Let Changesets own prerelease numbering

SheetOM's first public prerelease will be the conventional `0.1.0-rc.0` generated by Changesets rather than a manually seeded `rc.1`. Avoiding an artificial unpublished version keeps Changesets as the sole version authority and removes a one-time exception from release history.
