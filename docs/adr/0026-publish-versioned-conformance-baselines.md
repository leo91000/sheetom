# Publish versioned conformance baselines

Each release will expose the named browser and Lightning CSS versions used for its differential suite and publish measured coverage plus known divergences. The compatibility promise is maximal measured behavior against that Compatibility Baseline rather than an unqualified or timeless claim of complete CSSOM compatibility.
