# Generate a Chromium supported-property manifest

Ordinary property-name acceptance will use a checked-in manifest generated by probing the release's stable Chromium baseline with candidates from Webref, Lightning CSS, and known legacy properties. Specifications and parser catalogs may seed generation but will not define runtime support, and the npm package will never invoke a browser to validate names.
