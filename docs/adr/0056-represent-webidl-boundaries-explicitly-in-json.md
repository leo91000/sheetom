# Represent WebIDL boundaries explicitly in JSON

Operation Fixtures use explicit argument arrays to distinguish omission from presence and tagged Boundary Values for `undefined`, non-finite numbers, BigInt, Symbol, and controlled coercion objects. Thrown errors are observed structurally by name and resulting state by default, because browser messages are not a stable compatibility interface.
