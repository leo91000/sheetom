# Support Node, Bun, and Deno through npm

The npm package will support Node.js, Bun, and Deno while keeping one portable JavaScript implementation over the `lightningcss` native addon. Each runtime will have conformance smoke coverage; Deno usage will document its required local `node_modules` mode and native-code permissions, while browser execution remains out of scope.
