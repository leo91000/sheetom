# Version zero-major compatibility corrections as minors

Within a `0.x` line, patch releases remain backward compatible, while public-interface breaks or browser-conformance corrections that change observable behavior require a new minor version and a migration note. This gives consumers a useful compatibility signal before 1.0 without freezing behavior that evidence later proves incorrect.
