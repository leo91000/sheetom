# Support only the latest zero-major line

Before 1.0, only the latest published SheetOM `0.x` minor and its active prereleases are a Supported Release. This gives consumers a clear upgrade target without committing a solo-maintained prerelease project to multiple backport branches.
