# Separate WPT mappings from operation fixtures

Operation Fixtures contain stable provenance identifiers and operations, while a separate WPT mapping manifest retains upstream path, exact subtest title, source blob SHA, coverage, translation method, and mapped fixture identifiers. Specification and WPT expectation evidence belongs to Compatibility Resolutions rather than being duplicated as fixture expectations.
