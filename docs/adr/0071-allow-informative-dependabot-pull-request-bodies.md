# Allow informative Dependabot pull request bodies

SheetOM will enable dependency alerts and security updates plus weekly grouped npm and GitHub Actions version pull requests, without automatic merging. Dependabot's generated security and changelog body is an explicit exception to the repository's empty pull-request-body convention because removing it would make dependency review less informed.
