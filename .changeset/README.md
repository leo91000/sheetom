# Changesets

Add a Changeset for changes to published behavior, compatibility promises, the public interface, dependencies, or runtime support. Documentation, tests, generated evidence, and private refactors may be classified `no-changeset` in their pull request.

Changesets prepares versions and `CHANGELOG.md`; it never publishes SheetOM.
