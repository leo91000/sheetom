---
"sheetom": minor
---

Remove the legacy JavaScript syntax engine and all JavaScript runtime dependencies. SheetOM now uses its repository-owned Rust engine exclusively, with no behavioral fallback.
