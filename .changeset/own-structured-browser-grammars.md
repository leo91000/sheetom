---
"sheetom": patch
---

Own `clip`, `dynamic-range-limit`, `animation-trigger`, and `position-area` as typed Chromium-compatible grammars, and reserve containing-rule depth so every accepted declaration remains safely serializable.
