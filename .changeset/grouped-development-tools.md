---
"sheetom": patch
---

Update Changesets CLI, N-API CLI, Webref CSS data, Playwright, Rollup, tsdown, Vite, and Webpack. Preserve the reviewed CSSOM serialization oracle independently from the newer browsers used for adapter and WASM consumer validation.
