---
"sheetom": patch
"@sheetom/wasm": patch
---

Preserve authored pending shorthands after longhand mutations, add resilient best-effort serialization diagnostics for states CSS text cannot represent exactly, and expose `serializeStrict()` for exact-only callers.
