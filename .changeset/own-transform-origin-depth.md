---
"sheetom": minor
---

Own the complete Chromium `transform-origin` grammar, including typed depth lengths and calculations, WebKit alias state, canonical axis ordering, explicit zero units, atomic invalid replacement, and safe round-trip serialization.
