---
"sheetom": patch
"@sheetom/wasm": patch
---

Add the explicit, ESM-only `@sheetom/wasm` backend with the same private Engine Binding, parser, resource limits, and browser-shaped facade as the native package.
